server {
    listen {{build.deployment.port}} default_server;
    listen [::]:{{build.deployment.port}} default_server;
    root {{build.deployPath}};
    index index.html;
    #server_name {{build.deployment.serverName}};
    location / {
        try_files $uri $uri/ =404;
    }
}
